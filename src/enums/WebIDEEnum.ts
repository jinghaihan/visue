export enum LayoutTypeEnum {
  TOP = 'top',
  RIGHT = 'right',
  LEFT = 'left',
}

export enum ResizerDirectionEnum {
  VERTICAL = 'vertical',
  HORIZONTAL = 'horizontal',
}

export enum ResizerTypeEnum {
  PERCENT = 'percent',
  PIXEL = 'pixel',
}
